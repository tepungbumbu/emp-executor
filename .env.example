# === File EMP
EMP_FILE_PATH=data/EMP.txt

# === Blockchain RPC
ACTIVE_CHAIN=ethereum
ACTIVE_RPC=https://mainnet.infura.io/v3/YOUR_INFURA_PROJECT_ID

# === Proxy
PROXY_USERNAME=username
PROXY_PASSWORD=password
PROXY_HOST=proxy.example.com
PROXY_PORT=8080

# === Vault
VAULT_ADDR=https://vault.example.com
VAULT_TOKEN=your-vault-token

# === GUI (Flask)
FLASK_PORT=5000
FLASK_DEBUG=true
